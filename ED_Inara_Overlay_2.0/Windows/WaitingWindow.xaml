<Window x:Class="ED_Inara_Overlay_2._0.Windows.WaitingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ED Inara Overlay 2.0 - Waiting for Target Application"
        Height="469" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow"
        Background="#1A1A1A"
        Foreground="White">

    <Grid Margin="20,20,20,75">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" MinHeight="43.94"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20" Width="226">
            <Ellipse Width="16" Height="16" Fill="#00B4FF" Margin="0,0,10,0"/>
            <TextBlock Text="ED INARA OVERLAY 2.0" 
                       FontSize="18" 
                       FontWeight="Bold" 
                       Foreground="#00B4FF"/>
        </StackPanel>

        <!-- Main Content -->
        <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Height="248" Width="426">
            <!-- Animated Loading Spinner -->
            <Grid Width="48" Height="48" Margin="0,0,0,20">
                <!-- Background Circle -->
                <Ellipse Width="44" Height="44" 
                         Stroke="#404040" 
                         StrokeThickness="3" 
                         Fill="Transparent"/>
                
                <!-- Animated Arc -->
                <Ellipse Width="44" Height="44" 
                         Stroke="#00B4FF" 
                         StrokeThickness="3" 
                         Fill="Transparent"
                         StrokeDashArray="31.4 31.4"
                         StrokeDashOffset="0"
                         StrokeStartLineCap="Round"
                         StrokeEndLineCap="Round"
                         RenderTransformOrigin="0.5,0.5">
                    <Ellipse.RenderTransform>
                        <RotateTransform x:Name="SpinnerRotation"/>
                    </Ellipse.RenderTransform>
                    <Ellipse.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard RepeatBehavior="Forever">
                                    <DoubleAnimation Storyboard.TargetName="SpinnerRotation"
                                                   Storyboard.TargetProperty="Angle"
                                                   From="0" To="360" Duration="0:0:1.5"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </Ellipse.Triggers>
                </Ellipse>
                
                <!-- Center Icon -->
                <Ellipse Width="12" Height="12" 
                         Fill="#00B4FF" 
                         HorizontalAlignment="Center" 
                         VerticalAlignment="Center"/>
            </Grid>

            <!-- Status Text -->
            <TextBlock x:Name="StatusText" 
                       Text="Waiting for target application..." 
                       FontSize="16" 
                       HorizontalAlignment="Center" 
                       Margin="0,0,0,10"
                       Foreground="White"/>

            <!-- Target Process Name -->
            <TextBlock x:Name="TargetProcessText" 
                       Text="Looking for: notepad.exe" 
                       FontSize="14" 
                       HorizontalAlignment="Center" 
                       Margin="0,0,0,20"
                       Foreground="#B0B0B0"/>
            <Border Background="#2A2A2A" 
                    BorderBrush="#404040" 
                    BorderThickness="1" 
                    CornerRadius="4" 
                    Padding="15"
                    Width="378" Height="104">
                <StackPanel>
                    <TextBlock Text="Instructions:" 
                               FontWeight="Bold" 
                               FontSize="14" 
                               Foreground="#00B4FF" 
                               Margin="0,0,0,8"/>

                    <TextBlock x:Name="InstructionsText" 
                               TextWrapping="Wrap" 
                               FontSize="12" 
                               Foreground="#D0D0D0" 
                               LineHeight="16"><Run Text="1. Launch "/><Run x:Name="TargetAppName" Text="Notepad" Foreground="#00FF00"/><Run Text=" application"/><LineBreak/><Run Text="2. The overlay will automatically attach when detected"/><LineBreak/><Run Text="3. You can close this window to exit the overlay"/></TextBlock>
                </StackPanel>
            </Border>

            <!-- Instructions -->
        </StackPanel>

        <!-- Progress Info -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,10" Width="216">
            <TextBlock Text="Checking every " FontSize="11" Foreground="#808080"/>
            <TextBlock Text="1 second" FontSize="11" Foreground="#00B4FF"/>
            <TextBlock Text=" for target process..." FontSize="11" Foreground="#808080"/>
        </StackPanel>

        <!-- Close Button -->
        <Button Grid.Row="3" 
                x:Name="CloseButton"
                Content="Exit Overlay" 
                Width="120" 
                HorizontalAlignment="Center"
                Background="#FF4444" 
                Foreground="White" 
                BorderThickness="0" 
                FontSize="12"
                FontWeight="Bold"
                Cursor="Hand"
                Click="CloseButton_Click" Margin="0,10,0,-33">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" 
                                        CornerRadius="4" 
                                        BorderThickness="0">
                                    <ContentPresenter HorizontalAlignment="Center" 
                                                    VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#FF6666"/>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter Property="Background" Value="#CC3333"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Button.Style>
        </Button>
    </Grid>
</Window>
